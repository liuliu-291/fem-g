#ifndef H_GMSHFEM_GMSHFEMDEFINES
#define H_GMSHFEM_GMSHFEMDEFINES

#cmakedefine HAVE_BESSEL
#cmakedefine HAVE_BOOST
#cmakedefine HAVE_GMSH
#cmakedefine HAVE_OPENMP
#cmakedefine HAVE_PETSC
#cmakedefine HAVE_PROFILE
#cmakedefine HAVE_ROBINHOOD
#cmakedefine HAVE_SLEPC
#cmakedefine HAVE_WIN32

#define GMSHFEM_MAJOR_VERSION     ${GMSHFEM_MAJOR_VERSION}
#define GMSHFEM_MINOR_VERSION     ${GMSHFEM_MINOR_VERSION}
#define GMSHFEM_PATCH_VERSION     ${GMSHFEM_PATCH_VERSION}
#define GMSHFEM_DATE              "${GMSHFEM_DATE}"
#define GMSHFEM_HOST              "${GMSHFEM_HOST}"
#define GMSHFEM_PACKAGER          "${GMSHFEM_PACKAGER}"
#define GMSHFEM_OS                "${GMSHFEM_OS}"
#define GMSHFEM_CONFIG_OPTIONS    "${GMSHFEM_CONFIG_OPTIONS}"
#define GMSHFEM_CXX_FLAGS         "${CMAKE_CXX_FLAGS}"
#define GMSHFEM_CMAKE_BUILD_TYPE  "${CMAKE_BUILD_TYPE}"

// Constants

#define GMSHFEM_DOF_FIELD_OFFSET                       ${GMSHFEM_DOF_FIELD_OFFSET}
#define GMSHFEM_DOF_MEMORY_POOL_SIZE                   ${GMSHFEM_DOF_MEMORY_POOL_SIZE}
#define GMSHFEM_FUNCTION_MEMORY_ALIGNMENT              ${GMSHFEM_FUNCTION_MEMORY_ALIGNMENT}
#define GMSHFEM_FUNCTION_MEMORY_OVERSIZE_TOLERENCE     ${GMSHFEM_FUNCTION_MEMORY_OVERSIZE_TOLERENCE}
#define GMSHFEM_FUNCTION_MEMORY_RESIDUAL               ${GMSHFEM_FUNCTION_MEMORY_RESIDUAL}
#define GMSHFEM_FUNCTION_MEMORY_SMALLBUFFER_SIZE       ${GMSHFEM_FUNCTION_MEMORY_SMALLBUFFER_SIZE}
#define GMSHFEM_NUMA_ALLOCATOR                         ${GMSHFEM_NUMA_ALLOCATOR}

#endif // H_GMSHFEM_GMSHFEMDEFINES
